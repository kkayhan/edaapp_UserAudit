apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: useraudit-data
  namespace: eda-system
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 500Mi # app-set: ${logStorageSize}
