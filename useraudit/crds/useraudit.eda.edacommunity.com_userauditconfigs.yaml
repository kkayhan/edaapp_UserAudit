---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: userauditconfigs.useraudit.eda.edacommunity.com
spec:
  group: useraudit.eda.edacommunity.com
  names:
    kind: UserAuditConfig
    listKind: UserAuditConfigList
    plural: userauditconfigs
    singular: userauditconfig
  scope: Cluster
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: UserAuditConfig is the Schema for the userauditconfigs API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: UserAuditConfigSpec defines the desired state of UserAuditConfig
            properties:
              pollIntervalSeconds:
                default: 300
                description: PollIntervalSeconds is how often (in seconds) the controller
                  polls for new events.
                maximum: 3600
                minimum: 60
                type: integer
              retentionMonths:
                default: 0
                description: RetentionMonths is how many months of logs to keep. 0
                  means unlimited.
                minimum: 0
                type: integer
            type: object
          status:
            description: UserAuditConfigStatus defines the observed state of UserAuditConfig
            properties:
              health:
                description: 'Health is the overall health: ok, degraded, or error.'
                type: string
              kcEventsProcessed:
                description: KcEventsProcessed is the total KC events since the controller
                  started.
                type: integer
              lastPollTime:
                description: LastPollTime is the timestamp of the last completed poll
                  cycle.
                type: string
              lastTransactionId:
                description: LastTransactionID is the watermark -- highest transaction
                  ID processed.
                type: integer
              lastUserEventMs:
                description: LastUserEventMs is the KC event watermark (epoch milliseconds).
                format: int64
                type: integer
              logFiles:
                description: LogFiles lists the log files currently on disk.
                items:
                  description: LogFileInfo describes a single log file on disk.
                  properties:
                    name:
                      description: Name is the log file name.
                      type: string
                    sizeBytes:
                      description: SizeBytes is the file size in bytes.
                      format: int64
                      type: integer
                  required:
                  - name
                  - sizeBytes
                  type: object
                type: array
              message:
                description: Message is a human-readable explanation of the current
                  health state.
                type: string
              subsystems:
                description: Subsystems reports the health of each data source.
                properties:
                  edaApi:
                    description: EdaApi is the health of the EDA API connection.
                    type: string
                  keycloakEvents:
                    description: KeycloakEvents is the health of the Keycloak events
                      connection.
                    type: string
                type: object
              transactionsProcessed:
                description: TransactionsProcessed is the total count since the controller
                  started.
                type: integer
              version:
                description: Version is the controller version string.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
